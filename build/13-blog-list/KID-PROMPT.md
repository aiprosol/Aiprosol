# KID PROMPT — Phase 3.3 · Blog List V2

**Creates** `/blog` with featured post hero, category filter chips, search bar, paginated grid (9 posts/page).

Handles `coverImage` in either plain string or `{stringValue: ...}` object form, so it renders before AND after the Phase 0.5 bulk fix runs.

## Acceptance

- [ ] All 19 posts visible across pages
- [ ] First post (or `featured: true` post if any) shown as large featured card spanning both columns
- [ ] Category chips filter in place
- [ ] Search filters by title + excerpt in real time
- [ ] Pagination works with 9 posts per page
- [ ] Page resets to 1 when filters change

## Paste this into Kid

> **Goal:** Add `/blog` index page.
>
> **Steps:**
> 1. Create `src/pages/BlogListPage.tsx`, paste the body below.
> 2. Register the route `/blog` mapped to this component.
> 3. Do not modify any other file.
>
> ```tsx
> // ─── PASTE BlogListPage.tsx (file body) HERE ───
> // (copy from /Users/user/Airprosol/build/13-blog-list/BlogListPage.tsx)
> ```
